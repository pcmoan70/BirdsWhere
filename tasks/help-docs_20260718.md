# Help/About: document all functions in all languages — 2026-07-18

The About doc (`about.html` per language, 15 langs) predates many features. Add a
block of new `<h4>` sections covering the undocumented functions, inserted before
each language's "Key settings (… ⚙)" heading (the ⚙ is a reliable per-language
anchor — 15/15).

## New sections (English source)
1. Legend controls & species order (probability ordering, days/mode/observer/clear)
2. Observer filter & lists (👤, cycle scope, hover-isolate, add-to-list, editor)
3. Detections list (☰, by time/species, filter, save/navigate)
4. Crosshair / read cursor (3 states)
5. Place search (🔍, fuzzy, recent, hold→stored locations)
6. Map points & saved lists (pins, trips, share links, Share map)
7. Stored locations (save w/ radius, Shift+scroll, fetch from many)
8. Offline maps (download view, manager, redownload)
9. Photos on checklist entries
10. More settings (fetch timeout, max points, reuse downloads, place-name density)

## Method
- Insert English block before en "Key settings" heading.
- 14 subagents translate the block per language (keep tags/entities/emoji/code,
  translate visible text, match existing doc style).
- Node script escapes (`"`→`\"`) and inserts each before that lang's ⚙ heading.
- Syntax-check strings.js, bump sw VERSION, commit.
